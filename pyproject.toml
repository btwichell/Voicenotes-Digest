[project]
name = "bootcamp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "anthropic>=0.94.0",
    "mcp>=1.27.2",
    "python-dotenv>=1.2.2",
]
